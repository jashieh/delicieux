import React from 'react';

export default class RecipeIndex extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {

    return(
      <div>
        <div>
          {/* Map Array of Index here, with picture and title, each linking to a model with recipeId */}
        </div>

      </div>
    );
  }
}